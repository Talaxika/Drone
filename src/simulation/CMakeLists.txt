# # Create a library for the simulation module
# add_library(simulation DroneSimulation.cpp FlightController.cpp)

# # Include headers from the global include directory
# target_include_directories(simulation PUBLIC ../../include/simulation)
